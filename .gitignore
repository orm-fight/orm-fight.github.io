# Generated by scripts/collect-stats.js + scripts/build-trends.js.
# The authoritative copy lives on the stats-history branch; the deploy-site
# workflow pulls it into the Pages artifact at build time. main never tracks it.
data/

# Pages build output (deploy-site workflow).
_site/
